# Linear Interaction

Use this skill for tracker updates in Linear.

Rules:

1. Resolve the target issue or project before mutating anything.
2. Use `{{LINEAR_PROJECT_ID}}` when creating new issues from a reusable recipe.
3. Comments should include outcome, links, and any remaining decisions.
4. Do not close or move an issue to done unless the requested work is actually complete.
5. If an API token is missing, ask the lead or operator for the correct secret instead of searching local files.
