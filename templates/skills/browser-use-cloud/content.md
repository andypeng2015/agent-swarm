# Browser Use Cloud

Use this skill for remote browser automation.

Rules:

1. State the target site, success condition, and maximum runtime.
2. Avoid submitting forms or making purchases unless explicitly authorized.
3. Poll with a timeout and save screenshots or recordings as artifacts.
4. Report blocked states with the final URL, visible error, and artifact link.
5. Keep `{{BROWSER_USE_API_KEY}}` in secrets.
